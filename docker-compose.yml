services:

  proxy:
    build: ./proxy
    ports:
      - "8443:8443"
    volumes:
      - ./certs:/certs:ro
      - fingerprint-logs:/data/fingerprints
    environment:
      LISTEN_HOST: "0.0.0.0"
      PROXY_PORT: "8443"
      BACKEND_HOST: "backend"
      BACKEND_PORT: "8080"
      CERT_FILE: "/certs/proxy.crt"
      KEY_FILE: "/certs/proxy.key"
      LOG_DIR: "/data/fingerprints"
      LOG_RETENTION_DAYS: "30"
    depends_on:
      backend:
        condition: service_started
    networks:
      - internal
    restart: unless-stopped

  backend:
    build: ./backend
    expose:
      - "8080"
    environment:
      BACKEND_PORT: "8080"
    networks:
      - internal
    restart: unless-stopped

networks:
  internal:
    driver: bridge

volumes:
  fingerprint-logs:
